#include<iostream>
using namespace std;
int main(){
    int A=1,B=2,C=3;
    int D=A+B+C;
    int E=(B-A)+C;
    int F=(C-A)+B;
    
     if (A>B&&C)
    {
        if (A < B && C )
           cout<<D;
 
        else if (B>A && C)
            cout<<E;
        else if (C>>A &&B )
            cout<<F;
    }
 
    return 0;
}